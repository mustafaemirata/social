rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Public read access
      allow read;
      // Write access only for authenticated users
      allow write: if request.auth != null;
    }
  }
}
